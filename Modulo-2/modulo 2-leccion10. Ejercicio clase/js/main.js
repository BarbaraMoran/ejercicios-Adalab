"use strict";

//1.Muestra los 2 perros antes de llamarlos. No funcionan los botones.

/*const img = document.querySelector(".js-img");
const img2 = document.querySelector(".js-img2");
const btn = document.querySelector(".js-dog");
const btn2 = document.querySelector(".js-dog2");

function getDogImage(pic) {
  fetch("https://dog.ceo/api/breed/chihuahua/images/random")
    .then((response) => response.json())
    .then((data) => {
      pic.src = data.message;
      pic.alt = "Un perro";
    });
}

btn.addEventListener("click", getDogImage(img));
btn2.addEventListener("click", getDogImage(img2));
*/
