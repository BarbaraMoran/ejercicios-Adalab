'use strict';
let introducedUser = 'Bárbara';
introducedUser = 'Julia';

if (introducedUser === 'Bárbara'|| introducedUser === 'Lucía') {
    console.log(`Bienvenida,${introducedUser}`);
}

else {
    console.log('Lo siento, pero el usuario que has introducido no está registrado');
}

